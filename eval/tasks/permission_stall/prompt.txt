Rename the module utils/textkit.py to utils/textutils.py and update every import and caller to match, then commit the change with a clear message.

Sanity check after the rename: python3 app.py and python3 -m unittest discover -s tests -t . should both still work.
